import React from 'react';
import { View } from 'react-native';
import { PSImageBackground } from '../components';

const Register = () => {
  return <PSImageBackground />;
};

export default Register;
